export default ({ children }) => (
  <div
    style={{
      margin: '24px auto',
      maxWidth: 700,
      textAlign: 'center',
      '@media (min-width: 750px)': { margin: '64px auto' },
    }}
  >
    {children}
  </div>
);

# A Video Blog

Add a description here!
